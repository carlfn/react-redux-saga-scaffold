
const initialState = {
    isLoading: false,
    startDate: new Date()
}

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}